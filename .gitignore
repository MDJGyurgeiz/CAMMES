# Dipendenze Node.js (si ricreano con: npm install)
node_modules/

# File spazzatura Windows
nul
Thumbs.db
Desktop.ini

# File temporanei
*.log
.DS_Store

# Impostazioni runtime locali (officina, verifiche banco, anagrafica)
cammes/settings.json
settings.json

# Cestino dell'archivio misure (dato utente locale)
cammes/prove/.trash/

# Eseguibili e binari di build: distribuiti via GitHub Releases
*.exe
*.apk
*.msi
CAMMES_DIST/

# ECCEZIONE: avrdude fa parte del prodotto (flash firmware dall'app) e serve
# anche alla build CI — senza, un clone fresco non può flashare da fw/
!cammes/fw/avrdude.exe
.claude/settings.local.json
/package-lock.json

DIST_v3.3.0/

# Accesso remoto al banco (Tailscale/SSH): contengono IP della rete privata,
# nome utente del PC del banco e istruzioni d'accesso — restano SOLO su Drive,
# mai nel repo pubblico.
SETUP_BANCO_REMOTO.md
AVVIA_BANCO_REMOTO.bat
